---
name: create-app
description: 必要な初期化を行って新しいアプリケーションディレクトリを作成します。ユーザーが「新しいアプリを作成して」「新しいプロジェクトを追加して」「新しいアプリケーションを初期化して」と求めたときに使用します。
---

# 役割: アプリケーションスキャフォールディングスペシャリスト

あなたは、確立された `_template` を使用して、このリポジトリ内に新しいアプリケーションの作成を自動化するエンジニアです。
このスキルは一貫性を保証し、新しいアプリをViteビルドシステムに自動的に統合します。

## 1. ワークフロー

以下の手順を体系的に実行してください：

1.  **コンセプトの策定と命名:** まず、どのようなアプリにするか（機能や目的）を深く検討してください。その後、そのコンセプトにふさわしいアプリ名を決定します。ユーザーに確認する必要はありません。名前はケバブケース（例：`my-new-app`）にしてください。
2.  **テンプレートの確認:** `_template` ディレクトリがリポジトリのルートに存在することを確認します。
3.  **テンプレートのクローン:** `_template` ディレクトリ全体を、選択したアプリ名を持つ新しいディレクトリにコピーします。
    - コマンド: `cp -r _template <app-name>`
4.  **プロジェクトの初期化:**
    - ルートディレクトリの `apps.json` に新しいアプリのエントリを追記します：
      - キーにアプリのディレクトリ名（例：`my-new-app`）を指定し、以下のプロパティを持たせます。
      - `"title"`: 人間が読める形式のアプリ名（例：`My New App`）。
      - `"version"`: 初期バージョン（例：`0.1.0`）。
      - `"description"`: 「どんなゲームなのか、ゲームのコアとなる面白さ」が伝わる簡潔な説明。
      - `"image"`: 使用するポータル用メイン画像。画像ファイルはルートの `public/thumbnails/` に配置し、値は `"/thumbnails/<app-name>.png"` のように指定してください。
    - 新しいディレクトリ内の `index.html` を更新します：
      - `<title>` タグを、人間が読める形式のアプリ名（例：`My New App`）に更新します。
    - 各種説明を `README.md` に記載します（ポータル表示には使用されません）。
    - **注意:** 各アプリディレクトリ内に個別の `package.json` は作成・配置しません。依存関係はすべてルートの `package.json` で管理します。
5.  **静的アセットの配置:**
    - 画像や音声などのアセットは `src/assets/` ディレクトリに配置し、TypeScript内で `import` して使用してください。
6.  **最終確認:**
    - リポジトリのルートから `npm run build` を実行して、新しいアプリがViteによって正しく検出され、ビルドされることを確認します。
    - ユーザーに完了を報告します。

## 2. 制約事項

- **命名:** ディレクトリ名には、英数字とハイフンのみを使用してください。
- **除外:** `_template` ディレクトリ自体の変更は絶対に行わないでください。
- **自動化:** `vite.config.ts` はすでに動的であるため、変更は必要ありません。
